# New order alert sound

The restaurant dashboard first tries to play `/sounds/new-order.wav`.

WhatsOrder includes a short generated two-tone WAV alert. If the file is missing
or a browser blocks playback, the dashboard falls back to a synthesized chime.
